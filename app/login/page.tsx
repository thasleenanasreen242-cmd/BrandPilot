"use client";
import {FormEvent,useEffect,useState} from "react";import Link from "next/link";import {supabase} from "../../lib/supabase";
export default function LoginPage(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[mode,setMode]=useState<"login"|"signup">("login");const[loading,setLoading]=useState(false);const[message,setMessage]=useState("");const[error,setError]=useState("");useEffect(()=>{supabase?.auth.getSession().then(({data})=>{if(data.session)window.location.href="/ai-audit"})},[]);async function submit(e:FormEvent){e.preventDefault();setError("");setMessage("");if(!supabase){setError("Supabase is not configured. Add NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY in Vercel.");return}setLoading(true);try{if(mode==="login"){const{error}=await supabase.auth.signInWithPassword({email,password});if(error)throw error;window.location.href="/ai-audit"}else{const{data,error}=await supabase.auth.signUp({email,password});if(error)throw error;if(data.session)window.location.href="/ai-audit";else setMessage("Account created. Check your email to confirm your account, then sign in.")}}catch(e){setError(e instanceof Error?e.message:"Authentication failed.")}finally{setLoading(false)}}return <main className="min-h-screen bg-[#050b14] px-5 py-16 text-white"><div className="mx-auto max-w-md"><Link href="/" className="text-sm text-cyan-400">← BrandPilot</Link><div className="mt-8 rounded-3xl border border-white/10 bg-white/[.03] p-8"><p className="text-xs uppercase tracking-[.3em] text-cyan-400">BrandPilot AI</p><h1 className="mt-3 text-4xl font-black">{mode==="login"?"Welcome back":"Create your account"}</h1><p className="mt-3 text-sm text-slate-400">Sign in to manage customer audits and AI marketing workspaces.</p><form onSubmit={submit} className="mt-8 space-y-4"><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="h-13 w-full rounded-xl border border-white/10 bg-black/30 px-4 outline-none"/><input type="password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="h-13 w-full rounded-xl border border-white/10 bg-black/30 px-4 outline-none"/><button disabled={loading} className="w-full rounded-xl bg-gradient-to-r from-blue-600 to-cyan-500 py-3 font-bold disabled:opacity-50">{loading?"Please wait…":mode==="login"?"Sign In":"Create Account"}</button></form>{error&&<p className="mt-4 rounded-xl bg-red-500/10 p-4 text-sm text-red-300">{error}</p>}{message&&<p className="mt-4 rounded-xl bg-emerald-500/10 p-4 text-sm text-emerald-300">{message}</p>}<button onClick={()=>{setMode(mode==="login"?"signup":"login");setError("");setMessage("")}} className="mt-6 w-full text-sm text-slate-400">{mode==="login"?"New to BrandPilot? Create an account":"Already have an account? Sign in"}</button></div></div></main>}
