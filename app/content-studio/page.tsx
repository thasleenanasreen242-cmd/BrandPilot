"use client";

import { FormEvent, useEffect, useState } from "react";
import Link from "next/link";

const modes=["Social Post","Reel Script","30-Day Calendar","Blog Brief","Email Campaign","Ad Copy"];
export default function ContentStudio(){
 const [mode,setMode]=useState("Social Post"),[topic,setTopic]=useState(""),[audience,setAudience]=useState(""),[offer,setOffer]=useState(""),[platform,setPlatform]=useState("Instagram"),[answer,setAnswer]=useState(""),[loading,setLoading]=useState(false),[error,setError]=useState("");
 useEffect(()=>{const p=new URLSearchParams(window.location.search);const m=p.get("mode"),t=p.get("topic");if(m&&modes.includes(m))setMode(m);if(t)setTopic(t);},[]);
 async function generate(e:FormEvent){e.preventDefault();if(!topic.trim())return;setLoading(true);setError("");setAnswer("");try{const audit=localStorage.getItem("brandpilot-latest-seo-audit");const prompt=`Act as BrandPilot's senior content strategist. Generate a ready-to-publish ${mode}. Topic: ${topic}. Target audience: ${audience||"business owners"}. Offer/CTA: ${offer||"contact BrandPilot"}. Platform: ${platform}. Use this SEO audit context when useful: ${audit||"none"}. For a social post include hook, caption, CTA and hashtags. For a reel include scene-by-scene script, on-screen text and CTA. For a 30-day calendar include date/day, topic, format, hook and CTA. For a blog brief include SEO title, primary keyword, secondary keywords, outline, intent and CTA. For email include subject, preview text, body and CTA. For ads include headlines, descriptions, primary text, CTA and audience. Be specific and avoid generic filler.`;const r=await fetch("/api/marketing",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({prompt,business:{name:"BrandPilot"}})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Generation failed");setAnswer(d.answer||"");}catch(err){setError(err instanceof Error?err.message:"Unable to generate content.");}finally{setLoading(false);}}
 function save(){if(!answer)return;const old=JSON.parse(localStorage.getItem("brandpilot-marketing-workspace")||"[]");old.unshift({id:crypto.randomUUID(),title:`${mode}: ${topic}`,type:"Content Studio",content:answer,createdAt:new Date().toLocaleString()});localStorage.setItem("brandpilot-marketing-workspace",JSON.stringify(old));setError("");}
 function download(){const b=new Blob([answer],{type:"text/plain"}),u=URL.createObjectURL(b),a=document.createElement("a");a.href=u;a.download=`brandpilot-${mode.toLowerCase().replace(/\s+/g,"-")}.txt`;a.click();URL.revokeObjectURL(u);}
 return <main className="min-h-screen bg-gradient-to-br from-black via-zinc-950 to-black px-5 py-10 text-white md:px-8 md:py-16"><div className="mx-auto max-w-7xl"><div className="flex flex-wrap justify-between gap-3"><Link href="/marketing-ai" className="text-sm text-blue-400">← Marketing AI</Link><Link href="/marketing-dashboard" className="text-sm text-gray-400">Command Center →</Link></div><header className="mt-10"><p className="text-xs uppercase tracking-[0.3em] text-blue-400">AI Content Studio</p><h1 className="mt-3 text-5xl font-black md:text-7xl">Create marketing content with Gemini.</h1><p className="mt-4 max-w-3xl text-lg leading-8 text-gray-400">Turn one idea into social posts, reels, calendars, blogs, emails and ad copy from one workspace.</p></header><section className="mt-10 grid gap-6 lg:grid-cols-[360px_1fr]"><aside className="rounded-3xl border border-white/10 bg-white/[0.03] p-6"><p className="text-xs uppercase tracking-widest text-gray-500">Content type</p><div className="mt-4 grid grid-cols-2 gap-2">{modes.map(x=><button key={x} onClick={()=>setMode(x)} className={`rounded-xl px-3 py-3 text-sm ${mode===x?"bg-blue-500 font-bold":"border border-white/10 text-gray-400"}`}>{x}</button>)}</div><form onSubmit={generate} className="mt-7 space-y-4"><input value={topic} onChange={e=>setTopic(e.target.value)} placeholder="Topic / campaign idea" className="w-full rounded-xl border border-white/10 bg-black/40 px-4 py-3 outline-none placeholder:text-gray-600 focus:border-blue-500"/><input value={audience} onChange={e=>setAudience(e.target.value)} placeholder="Target audience" className="w-full rounded-xl border border-white/10 bg-black/40 px-4 py-3 outline-none placeholder:text-gray-600 focus:border-blue-500"/><input value={offer} onChange={e=>setOffer(e.target.value)} placeholder="Offer / CTA" className="w-full rounded-xl border border-white/10 bg-black/40 px-4 py-3 outline-none placeholder:text-gray-600 focus:border-blue-500"/><select value={platform} onChange={e=>setPlatform(e.target.value)} className="w-full rounded-xl border border-white/10 bg-black/40 px-4 py-3 outline-none"><option>Instagram</option><option>LinkedIn</option><option>Facebook</option><option>Google</option><option>Email</option><option>Website</option></select><button disabled={loading||!topic.trim()} className="w-full rounded-xl bg-blue-500 px-5 py-4 font-bold hover:bg-blue-600 disabled:opacity-50">{loading?"Gemini is creating…":"✦ Generate Content"}</button></form></aside><section className="min-h-[500px] rounded-3xl border border-white/10 bg-white/[0.03] p-6 md:p-8"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs uppercase tracking-widest text-blue-400">Generated Output</p><h2 className="mt-2 text-2xl font-bold">{mode}</h2></div>{answer&&<div className="flex gap-2"><button onClick={save} className="rounded-lg border border-white/10 px-4 py-2 text-sm">Save</button><button onClick={download} className="rounded-lg border border-white/10 px-4 py-2 text-sm">Export</button></div>}</div>{error&&<p className="mt-6 rounded-xl border border-red-500/20 bg-red-500/10 p-4 text-red-300">{error}</p>}{!answer&&!loading&&!error&&<div className="flex min-h-[400px] items-center justify-center text-center text-gray-600"><div><div className="text-5xl">✦</div><p className="mt-4">Choose a content type and give Gemini an idea.</p></div></div>}{loading&&<div className="mt-8 animate-pulse text-gray-400">Creating your marketing asset…</div>}{answer&&<article className="mt-7 whitespace-pre-wrap leading-7 text-gray-300">{answer}</article>}</section></section><section className="mt-8 grid gap-4 md:grid-cols-3"><div className="rounded-2xl border border-white/10 p-6"><p className="text-blue-400">SEO-aware</p><h3 className="mt-2 font-bold">Uses your latest audit</h3><p className="mt-2 text-sm text-gray-500">Content generation can use the SEO audit stored by your BrandPilot workflow.</p></div><div className="rounded-2xl border border-white/10 p-6"><p className="text-blue-400">Multi-channel</p><h3 className="mt-2 font-bold">One idea, many formats</h3><p className="mt-2 text-sm text-gray-500">Turn campaigns into social, email, blog and advertising assets.</p></div><div className="rounded-2xl border border-white/10 p-6"><p className="text-blue-400">Workspace</p><h3 className="mt-2 font-bold">Save and reuse</h3><p className="mt-2 text-sm text-gray-500">Save generated assets to your Marketing Workspace for later execution.</p></div></section></div></main>;
}
