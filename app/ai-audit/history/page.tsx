"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
type Audit={id:string;url:string;hostname:string;name:string;overall:number;seo:number;performance:number;content:number;technical:number;date:string;data:any};
export default function AuditHistory(){const[items,setItems]=useState<Audit[]>([]);useEffect(()=>{try{setItems(JSON.parse(localStorage.getItem("brandpilot-customer-audit-history")||"[]"))}catch{setItems([])}},[]);function clear(){localStorage.removeItem("brandpilot-customer-audit-history");setItems([])}return <main className="min-h-screen bg-[#050b14] px-5 py-12 text-white sm:px-8"><div className="mx-auto max-w-6xl"><div className="flex flex-wrap justify-between gap-4"><Link href="/ai-audit" className="text-sm text-blue-400">← SEO Audit</Link><button onClick={clear} disabled={!items.length} className="text-sm text-gray-500 disabled:opacity-30">Clear history</button></div><header className="mt-10"><p className="text-xs uppercase tracking-[0.3em] text-blue-400">Customer Intelligence</p><h1 className="mt-3 text-5xl font-black">SEO Audit History</h1><p className="mt-4 text-gray-400">Reopen previous customer website audits from this browser.</p></header>{!items.length?<div className="mt-10 rounded-3xl border border-white/10 bg-white/[0.03] p-10 text-center"><p className="text-gray-400">No customer audits saved yet.</p><Link href="/ai-audit" className="mt-5 inline-block rounded-xl bg-blue-500 px-5 py-3 font-bold">Audit a Customer →</Link></div>:<div className="mt-10 grid gap-4">{items.map(x=><Link key={x.id} href={`/ai-audit?audit=${encodeURIComponent(x.id)}`} className="rounded-3xl border border-white/10 bg-white/[0.03] p-6 transition hover:border-blue-400/30"><div className="flex flex-col gap-5 md:flex-row md:items-center md:justify-between"><div><p className="text-2xl font-bold">{x.name}</p><p className="mt-1 break-all text-sm text-gray-500">{x.url}</p><p className="mt-3 text-xs text-gray-600">{new Date(x.date).toLocaleString()}</p></div><div className="flex gap-5 text-center"><Score label="Overall" value={x.overall}/><Score label="SEO" value={x.seo}/><Score label="Technical" value={x.technical}/></div></div></Link>)}</div>}</div></main>}
function Score({label,value}:{label:string;value:number}){return <div><p className="text-[10px] uppercase tracking-widest text-gray-600">{label}</p><p className={`mt-1 text-3xl font-black ${value>=75?"text-cyan-400":value>=60?"text-yellow-400":"text-red-400"}`}>{value}</p></div>}
