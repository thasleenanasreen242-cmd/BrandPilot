import { NextResponse } from "next/server";
const MODEL="gemini-2.5-flash";
export async function POST(request:Request){try{const key=process.env.GEMINI_API_KEY;if(!key)return NextResponse.json({error:"GEMINI_API_KEY is not configured in Vercel."},{status:500});const{customer,competitors}=await request.json();if(!customer?.website)return NextResponse.json({error:"Customer website is required."},{status:400});const prompt=`You are BrandPilot's Competitor Intelligence Employee. Analyze the customer's business using the supplied customer details and competitor URLs/names. Do not invent factual competitor claims. Clearly label assumptions. Return ONLY JSON: {"marketPosition":"string","competitors":[{"name":"string","website":"string","strengths":["string"],"weaknesses":["string"],"opportunities":["string"]}],"gaps":["string"],"seoOpportunities":["string"],"contentOpportunities":["string"],"adOpportunities":["string"],"actions":["string"]}. CUSTOMER: ${JSON.stringify(customer)} COMPETITORS: ${JSON.stringify(competitors||[])}`;const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${MODEL}:generateContent?key=${encodeURIComponent(key)}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],generationConfig:{temperature:.35,responseMimeType:"application/json"}})});const d=await r.json();if(!r.ok)return NextResponse.json({error:d?.error?.message||"Gemini competitor analysis failed."},{status:r.status});const text=d?.candidates?.[0]?.content?.parts?.map((p:{text?:string})=>p.text||"").join("")||"";let result;try{result=JSON.parse(text)}catch{const m=text.match(/\{[\s\S]*\}/);result=m?JSON.parse(m[0]):null}if(!result)return NextResponse.json({error:"Gemini returned invalid competitor data."},{status:502});return NextResponse.json({success:true,result});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Unable to analyze competitors."},{status:500})}}